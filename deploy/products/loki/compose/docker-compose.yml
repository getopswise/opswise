services:
  loki:
    image: grafana/loki:${LOKI_VERSION:-2.9.6}
    container_name: loki
    restart: unless-stopped
    ports:
      - "${LOKI_PORT:-3100}:3100"
    volumes:
      - loki_data:/loki

volumes:
  loki_data:
