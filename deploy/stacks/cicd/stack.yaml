name: cicd
display_name: CI/CD Stack
description: GitLab CE + Harbor for source control and container registry
products:
  - name: gitlab
    config:
      external_url: "{{ .GitLabURL }}"
      port: 80
  - name: harbor
    config:
      port: 80
      admin_password: "{{ .HarborAdminPassword }}"
