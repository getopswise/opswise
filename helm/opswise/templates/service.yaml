apiVersion: v1
kind: Service
metadata:
  name: {{ include "opswise.fullname" . }}
  labels:
    {{- include "opswise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opswise.selectorLabels" . | nindent 4 }}
